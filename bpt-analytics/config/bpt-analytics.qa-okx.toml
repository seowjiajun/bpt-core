# Tyr — Toxic Flow Analyzer
# Passive observer: subscribes to exec reports + MD, publishes toxicity scores.
# Does NOT interfere with trading — fenrir optionally subscribes to stream 5001.

# Shared aeron stream registry — see deploy/config/aeron/streams.toml.
# Deployment profile (env only; analytics doesn't open exchange conns).
profile_config = "../deploy/config/profile/qa-okx.toml"

aeron_config = "../deploy/config/aeron/streams.toml"

[aeron]
# Fallback only — shared file wins when present.
media_driver_dir = "/dev/shm/aeron-bpt"

[tyr]
markout_max_pending   = 64
scorer_window_size    = 50
scorer_min_samples    = 5
publish_interval_ms   = 5000

[logging]
level = "info"
dir   = "logs"

[metrics]
port = 9105
